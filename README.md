Задания:
(Да часть из этих объектов нет в видео, что я дал, это специально, чтоб вы хотяб часть инфы учились сами искать)
Описано загадками, чтоб запоминалось для собеса. Если непонятно, то разбираешь по части предложения 
  "Так, какой объект имеет такие характеристики? Не знаю, уточню у нейронки или погуглю"
1) Написать манифест:
- Создать пространство "study"
- Создать в "study" объект, который в себе содежит возможности ReplicaSet и также добавляет возможность версионирования с 2 репликами.  Pod должен содержать 1 Init контейнер с любым образом и основным контейнером с образом Nginx
- Создать объект для конфигруации, в котором будет лежать конфиг Nginx(сами его написать или можно взять из задания из блока "Сети") и примонтировать к нашему Pod с Nginx
- Сделать у основного контейнера Burstable QoS

- В Init контейнере создавать файл, в котором будет текст "Some Test" и этот файл будет перенесен в основной контейнер путем монтирования volume 
- Создать объект, который позволит увеличивать количество replic нашего Pod в случае если CPU привышает 80%, допустим минмально чтоб было 2, а максимум 5
(Из интернета не будет доступен в любом случае, пока только добавить объекты)
__
  Через команду kubectl port-forward .... пробросить порт и посмотреть в браузере результат (Если вы на виртуалке, то открыть браузер в ней, а не у себя локально, кек)
  💫 Отправить 1скрином - браузер и манифест объекта


2) Создать объект K8s, который позволит держать 2 реплики баз данных - stateful приложений
  💫 Скинь скрин манифеста

3) Создать объект, который создает по 1 pod  на каждую node
- Создать объект для хранения секретных данных, вписать туда любую переменную и значение к ней и примонтировать в наш Pod как переменную окружения. Допустим SECRET = "SECRETNIY_SECRET"
_
  💫 Скинуть скрин команды однострочника, где через kubectl exec ...  выводится переменная окружения
